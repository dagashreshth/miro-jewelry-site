/* ============================================================
   Miró — Published catalogue overrides
   Written by the back office ("Publish to live site") and committed to the
   repository. store.js layers this over the built-in catalogue, so whatever
   is here wins on the live storefront.

   Generated file — edit through the dashboard, not by hand.
   Safe to leave empty: the static catalogue is used as-is.
   ============================================================ */
window.MiroCatalog = {
  updatedAt: null,
  pieces: {},
  instagram: []
};
